import 'package:flutter/material.dart';

class SpalshPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("APP Mobaul Pasar"),
      ),
    );
  }
}